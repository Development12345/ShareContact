package com.nasatrainedmonkeys.sharecontact;

import roboguice.config.AbstractAndroidModule;

public class IoCModule extends AbstractAndroidModule {
	
	@Override
	protected void configure() {
		//bind(IConnectionStatus.class).to(ConnectionStatus.class);
	}
}
